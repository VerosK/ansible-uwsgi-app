# {{ ansible_managed }}
# This is fake wsgi app

counter = 0

def application(environ, start_response):
    global counter

    start_response('200 OK', [('Content-Type', 'text/plain')])
    yield 'Hello WSGI.\n Request count: {} \n'.format(counter)
    counter += 1
